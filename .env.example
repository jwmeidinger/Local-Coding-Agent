# =============================================================================
# Coding Agent – Environment variables
# Copy to .env and adjust. Used by run.sh and (for DB/embeddings) by the agent.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (vector memory / codebase indexing)
# Optional: only needed if you use docker-compose for PostgreSQL + pgvector.
# -----------------------------------------------------------------------------
# Host of the PostgreSQL server
DB_HOST=localhost
# Port of the PostgreSQL server
DB_PORT=5432
# Database name (must match the one created in init.sql / docker)
DB_NAME=coding_agent
# PostgreSQL user
DB_USER=agent
# PostgreSQL password (change in production)
DB_PASSWORD=agent_password

# -----------------------------------------------------------------------------
# LLM (used by run.sh and passed to the agent)
# -----------------------------------------------------------------------------
# URL of the LLM API (Ollama default; use same for LM Studio with its port)
LLM_URL=http://localhost:11434
# Model name (e.g. codellama, llama3, mistral). Must be loaded in Ollama/LM Studio.
# Ollama: tag is optional (codellama and codellama:latest are the same); use :7b, :8b for a specific variant.
# LM Studio: use the model name/ID shown in the app or from run.sh --list-models.
MODEL=codellama
# Optional: bind outgoing LLM requests to this local IP (e.g. to bypass VPN).
# Use your machine’s IP on the interface that can reach the LLM (e.g. Wi‑Fi).
# SOURCE_IP=10.10.50.103

# -----------------------------------------------------------------------------
# run.sh only – repository and git behavior
# -----------------------------------------------------------------------------
# Git repo path to run the agent in (default: current directory when you run run.sh)
# REPO_PATH=/path/to/your/project
# Base branch to branch from (default: main)
# BASE_BRANCH=main
# Prefix for agent-created branches (default: agent/)
# BRANCH_PREFIX=agent/

# -----------------------------------------------------------------------------
# run.sh only – agent behavior
# -----------------------------------------------------------------------------
# Max planning/execution iterations per task (default: 5)
# MAX_ITERATIONS=5
# Set to true to enable verbose logging
# VERBOSE=false
# Path to coding_agent.py (default: same directory as run.sh)
# AGENT_SCRIPT=/path/to/coding_agent.py

# -----------------------------------------------------------------------------
# Optional: better embeddings (vector memory)
# If set, the agent can use OpenAI for semantic embeddings instead of fallback.
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=your-api-key-here
