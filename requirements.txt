# Core dependencies
GitPython>=3.1.40
psycopg2-binary>=2.9.9
ddgs>=9.11.2
pytest>=8.0.0

# Optional: For better embeddings (OpenAI)
# openai>=1.0.0

# Optional: For local embeddings (better than fallback)
# sentence-transformers>=2.2.0

# Optional: Premium web search backend
# exa-py>=1.0.0
